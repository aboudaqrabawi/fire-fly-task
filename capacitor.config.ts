import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'fire-fly-task',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
